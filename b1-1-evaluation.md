# B1-1 평가 항목

> **미션:** 컴퓨터가 알아서 자기 상태를 점검하게 만들기  
> **분야:** Linux와 OS  
> **평가 범위:** 시스템 보안, 계정·권한, 애플리케이션 실행, 시스템 관제, 로그 관리, 장애 대응

각 문항을 확인한 뒤 충족한 항목에 체크합니다.

---

## 항목 1. 요구사항 구현 및 동작 확인

- [ ] SSH 포트가 `20022`로 변경되었고, Root 원격 접속이 차단되었는가?
- [ ] 방화벽이 활성화되어 있고(UFW 또는 firewalld 중 택1), `20022/tcp`와 `15034/tcp`만 허용되는가?
- [ ] `agent-admin`, `agent-dev`, `agent-test` 계정과 `agent-common`, `agent-core` 그룹이 요구사항대로 구성되어 있는가?
- [ ] 애플리케이션이 Boot Sequence 5단계 `[OK]`를 통과하고 `Agent READY`가 출력되는가?
- [ ] `monitor.sh`가 프로세스와 포트 상태를 점검하고, 비정상 상태에서 `exit 1`로 종료되는가?
- [ ] `/var/log/agent-app/monitor.log`가 지정된 포맷으로 누적 기록되는가?
- [ ] cron이 매분 실행되어 `monitor.log`가 자동으로 증가하는가?
- [ ] `monitor.log` 용량 관리 정책인 `10MB / 10개`가 설정되어 있고, 그 동작을 설명할 수 있는가?

---

## 항목 2. 구현 방식 및 명령어 설명

- [ ] `monitor.sh`에서 프로세스 식별에 사용한 명령어(`pgrep`, `ps` 등)와 포트 확인에 사용한 명령어(`ss`, `netstat` 등), 그리고 해당 명령어를 선택한 이유를 설명할 수 있는가?
- [ ] CPU, MEM, DISK 값을 어떤 방식으로 추출하고 파싱했으며, 로그 포맷을 왜 해당 형태로 고정했는지 설명할 수 있는가?
- [ ] 소유자 `agent-dev`와 실행자 `agent-admin`, cron 실행 환경의 권한 정책을 소유자·그룹·권한 관점에서 어떻게 만족시켰는지 설명할 수 있는가?
- [ ] 용량 기반 로그 관리 정책인 `10MB / 10개`를 `logrotate` 또는 스크립트 방식으로 어떻게 구현했는지 설명할 수 있는가?

---

## 항목 3. 보안·권한·운영 원리 설명

- [ ] SSH 포트 변경과 Root 원격 접속 차단이 왜 보안에 효과적인지 위협 모델 관점에서 설명할 수 있는가?
- [ ] `api_keys`와 로그 디렉터리의 접근을 `agent-core` 그룹으로 제한한 이유를 최소 권한 원칙으로 설명할 수 있는가?
- [ ] 방화벽 비활성 또는 자원 임계치 초과와 같이 경고는 출력하되 스크립트를 종료하지 않는 항목을 분리한 운영상의 이유를 설명할 수 있는가?
- [ ] 리다이렉션 기호 `>`와 `>>`의 차이를 설명하고, 로그 누적에 `>>`가 필요한 이유를 설명할 수 있는가?

---

## 항목 4. 응용 및 장애 대응

- [ ] 모니터링 대상이 웹 서버(Nginx 등)로 변경될 경우, `monitor.sh`에서 변경해야 할 핵심 항목인 프로세스, 포트, 로그, 임계값을 설명할 수 있는가?
- [ ] 프로세스는 실행 중이지만 포트가 열리지 않는 상황을 발견했을 때, 가능한 원인과 확인 순서를 설명할 수 있는가?
- [ ] 로그가 급증하여 디스크가 가득 찰 위험이 있을 때, 운영자가 취해야 할 단기 대응과 중기 대응을 설명할 수 있는가?

---

## 평가 결과 기록

| 평가 항목 | 충족 | 부분 충족 | 미충족 | 비고 |
|---|:---:|:---:|:---:|---|
| 항목 1. 요구사항 구현 및 동작 확인 | [ ] | [ ] | [ ] | |
| 항목 2. 구현 방식 및 명령어 설명 | [ ] | [ ] | [ ] | |
| 항목 3. 보안·권한·운영 원리 설명 | [ ] | [ ] | [ ] | |
| 항목 4. 응용 및 장애 대응 | [ ] | [ ] | [ ] | |

---

## 최종 확인

- [ ] 모든 설정과 실행 결과를 명령어 출력 또는 화면 캡처로 증빙할 수 있다.
- [ ] `monitor.sh`, cron, 로그 누적, 로그 회전이 실제 환경에서 동작한다.
- [ ] 구현 내용뿐 아니라 설계 이유와 장애 대응 절차를 말로 설명할 수 있다.
